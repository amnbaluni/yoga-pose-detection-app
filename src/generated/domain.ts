// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Domain Store
 *
 * MobX-State-Tree store with collections for each model.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import { makeAutoObservable, runInAction } from 'mobx'
import {
  getUserList,
  getUserById,
  createUser,
  updateUser,
  deleteUser,
} from './server-functions'

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
} from './types'

export class UserStore {
  items: Map<string, UserType> = new Map()
  isLoading = false
  error: string | null = null

  private pendingDeletes = new Set<string>()
  private pendingUpdates = new Set<string>()

  constructor() {
    makeAutoObservable(this, {
      pendingDeletes: false,
      pendingUpdates: false,
    })
  }

  get all(): UserType[] {
    return Array.from(this.items.values())
  }

  get(id: string): UserType | undefined {
    return this.items.get(id)
  }

  isPending(id: string): boolean {
    return this.pendingDeletes.has(id) || this.pendingUpdates.has(id)
  }

  async loadAll(userId?: string, where?: Record<string, unknown>) {
    runInAction(() => { this.isLoading = true; this.error = null })
    try {
      const items = await getUserList({ data: { userId, where } })
      runInAction(() => {
        this.items.clear()
        for (const item of items) { this.items.set(item.id, item) }
        this.isLoading = false
      })
    } catch (e) {
      runInAction(() => { this.error = e instanceof Error ? e.message : 'Failed to load'; this.isLoading = false })
      throw e
    }
  }

  async loadById(id: string, userId?: string) {
    try {
      const item = await getUserById({ data: { id, userId } })
      runInAction(() => { this.items.set(item.id, item) })
      return item
    } catch (e) {
      runInAction(() => { this.error = e instanceof Error ? e.message : 'Failed to load' })
      throw e
    }
  }

  async create(input: UserCreateInput, userId?: string) {
    const tempId = `temp-${crypto.randomUUID()}`
    const optimisticItem: UserType = { id: tempId, ...input, createdAt: new Date(), updatedAt: new Date() } as UserType
    runInAction(() => { this.items.set(tempId, optimisticItem) })
    try {
      const item = await createUser({ data: { input, userId } })
      runInAction(() => { this.items.delete(tempId); this.items.set(item.id, item) })
      return item
    } catch (e) {
      runInAction(() => { this.items.delete(tempId); this.error = e instanceof Error ? e.message : 'Failed to create' })
      throw e
    }
  }

  async update(id: string, input: UserUpdateInput, userId?: string) {
    if (!id || typeof id !== 'string') return
    const existing = this.items.get(id)
    if (!existing || this.pendingUpdates.has(id)) return
    const previousState = { ...existing }
    this.pendingUpdates.add(id)
    runInAction(() => { this.items.set(id, { ...existing, ...input, updatedAt: new Date() } as UserType) })
    try {
      const item = await updateUser({ data: { id, input, userId } })
      runInAction(() => { this.items.set(id, item); this.pendingUpdates.delete(id) })
      return item
    } catch (e) {
      runInAction(() => { this.items.set(id, previousState); this.pendingUpdates.delete(id); this.error = e instanceof Error ? e.message : 'Failed to update' })
      throw e
    }
  }

  async delete(id: string, userId?: string) {
    if (!id || typeof id !== 'string') return
    const existing = this.items.get(id)
    if (!existing || this.pendingDeletes.has(id)) return
    this.pendingDeletes.add(id)
    runInAction(() => { this.items.delete(id) })
    try {
      await deleteUser({ data: { id, userId } })
      runInAction(() => { this.pendingDeletes.delete(id) })
    } catch (e) {
      runInAction(() => { this.items.set(id, existing); this.pendingDeletes.delete(id); this.error = e instanceof Error ? e.message : 'Failed to delete' })
      throw e
    }
  }

  clearError() { this.error = null }
  clear() { this.items.clear(); this.error = null; this.isLoading = false }
}

export class RootStore {
  user: UserStore
  constructor() { this.user = new UserStore(); makeAutoObservable(this) }
  clearAll() { this.user.clear() }
}

let rootStore: RootStore | null = null
export function getStore(): RootStore { if (!rootStore) rootStore = new RootStore(); return rootStore }
export function createStore(): RootStore { return new RootStore() }
export function resetStore(): void { rootStore = null }